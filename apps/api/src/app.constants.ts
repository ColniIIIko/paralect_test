const DATABASE_DOCUMENTS = {
  USERS: 'users',
  TOKENS: 'tokens',
  PRODUCTS: 'products',
  ORDERS: 'orders',
};

const COOKIES = {
  ACCESS_TOKEN: 'access_token',
};

const TOKEN_SECURITY_LENGTH = 32;

export { COOKIES, DATABASE_DOCUMENTS, TOKEN_SECURITY_LENGTH };
